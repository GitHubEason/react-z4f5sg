import React from "react";
import { Controller, useForm } from "react-hook-form";
import NumberFormat from "react-number-format";

const TaxFormat = () => {
  const { control } = useForm({ amount: 0 });
  return (
    <div>


    </div>
  );
};

export default TaxFormat;